<!DOCTYPE html>
<html>
<head>
  <title>GA</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

	<br>
   <% if current_user %>
      <p> Welcome, <%= current_user.username %></p>
  <br>
  
  <%= link_to 'Logout', session_path(session), method: :delete, data: {confirm: 'Are you sure?'} %><br>
    <% else %>
      <%= link_to ' Please login', login_path %><br>
      <%= link_to ' Please register', new_user_path %>
  <%end%>
  <br>

  <% flash.each do |name, message| %>
  <div class='flash-message flash-message- <%= name %>'>
    <%= message %>
  </div>
    <%end%>

    <nav>
    	<ul>
    		<li><%= link_to 'Courses', courses_path(@courses) %></li>
    	</ul>
    </nav>

<%= yield %>

</body>
</html>
